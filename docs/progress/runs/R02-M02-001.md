# R02-M02-001 工程底座最小可运行闭环

## 任务与边界

- 任务编号：`R02-M02-001`
- 目标：在现有 `apps/server` 和 `apps/web` 上完成真实 PostgreSQL、受控 Flyway 迁移、健康/版本接口、状态工作台和自动化验证。
- 不在范围：登录、身份权限、需求/用例/缺陷、执行 Agent、OTA/COM、电子签名、AI、完整 M02/P0 和生产部署。
- 记录性质：本记录是执行证据，不是外部审核通过声明。

## 起始状态

- 仓库：`https://github.com/fangzhiy/Test365Alm.git`
- 远端基准：`origin/chore/r01-status-001`
- 起始分支：`chore/r01-status-001`
- 起始提交：`cff62dfed99c698a2e32bea85de17e224761eb54`
- 起始工作区：重新 `git fetch origin --prune` 后干净；远端未发现 `main`，也未发现晚于起始 SHA 的任务分支提交。
- 本轮分支：`feat/r02-m02-001`，从真实远端基准创建。

## 工具、运行时和配置

| 项目 | 实际值 | 结论 |
|---|---|---|
| Java | 17.0.2 | 本机可用；Java 21 目标未安装，待目标环境复跑 |
| Node/npm | v26.0.0 / 11.12.1 | 本机可用；Node 24 LTS 为 CI/目标版本 |
| Docker/Compose | Engine 29.7.2 / Compose v5.4.0 | 本地 PostgreSQL 验证可用 |
| Maven | 系统 3.9.14；Wrapper 运行 3.9.16 | 验证命令使用 Wrapper |
| PostgreSQL | 17.11，digest `sha256:f4c66b820c6f974249089d3d16d86a3698eae11e8746eb6644b2271031e91232` | Compose 仅监听 127.0.0.1 |
| 凭据 | `.env`（被忽略）及测试环境变量 | 示例值为本地开发占位符，未提交真实凭据 |

完整选型及偏差见 `docs/adr/006-r02-runtime-and-image-lock.md`；接口语义见 `docs/adr/007-platform-health-contract.md`。

## 实际完成内容

- 后端：`/health/live`、`/health/ready`、`/api/v1/version`；readiness 对 PostgreSQL 与 `platform_metadata` 做有界探测，故障使用 HTTP 503；版本使用 Spring Boot build metadata，缺少 Git 元数据时为 `unknown`。
- 数据库：`compose.yaml`、`.env.example`、Flyway `V1__platform_metadata.sql`；迁移唯一维护在 `apps/server/src/main/resources/db/migration`。
- 前端：替换 Vite 示例页为真实状态工作台，使用相对路径和本地 Vite proxy；显示版本、构建提交、liveness、readiness，处理加载、503、超时/网络失败和恢复。
- 验证：后端 MockMvc 单元测试、真实 PostgreSQL Failsafe 集成测试；前端 Vitest/Testing Library 测试；CI workflow 覆盖规划工具、前端和后端。
- 文档：README 启动/检查/停止/排障、AGENTS 命令和边界、当前状态、ADR 与本轮记录。

## A01-A10 验收矩阵

| 编号 | 结果 | 证据/说明 |
|---|---|---|
| A01 | PASS（本机偏差） | `npm ci`、前端构建、Maven Wrapper 构建均实际执行；本机是 Java17/Node26，Java21/Node24 待目标环境复跑。 |
| A02 | PASS | 隔离 Compose PostgreSQL 空库由 Flyway 应用 V1；证据为 `PlatformDatabaseIT` 与 Maven 日志。 |
| A03 | PASS | 集成测试真实请求 live/ready 均 HTTP 200；readiness body 含 `UP/UP/APPLIED`。 |
| A04 | PASS | 打包应用以 `18080` 运行（8080 已被其他本机进程占用且未触碰）；停止隔离 PostgreSQL 后 live `200`、ready `503`，body 为 `{"status":"DOWN","database":"DOWN","migration":"UNKNOWN"}`。 |
| A05 | PASS | 启动同一隔离 PostgreSQL、保持后端进程不重启，ready 恢复 `200`；后端版本接口继续返回 `Test365Alm/0.0.1-SNAPSHOT/44e6914`。 |
| A06 | PASS | 前端 Vitest 4/4；真实 Vite proxy `/api/v1/version` 返回后端 JSON、live/ready 为 200；隔离数据库停止时浏览器 DOM 显示“应用就绪 不可用 / 数据库 DOWN · 迁移 UNKNOWN”，恢复后点击刷新显示“正常 / 数据库 UP · 迁移 APPLIED”，浏览器控制台 error/warn 为空。 |
| A07 | PASS | Failsafe 第二次运行显示 Flyway “Schema public is up to date. No migration necessary.”；测试更新元数据后再次 migrate 保留值。 |
| A08 | PASS | 单测 5 个、集成测试 2 个、前端测试 4 个均有执行计数；未用零测试退出码冒充通过。 |
| A09 | PASS（仓库扫描） | `.env` 被忽略；`git diff --check` 无空白错误；提交内容未包含密码、令牌或私钥。仍需外部审核复核。 |
| A10 | PASS | GitHub Actions run `35709638396` 对 SHA `3bcb16b5bc7a107ea2adfa526cf9f70c5361d756` 已完成；`planning-tools`、`web`、`server` 三个 job 均 success。 |

## 实际命令与结果

以下命令均在本轮实际执行；路径均相对于仓库根目录，退出码以终端返回为准。

1. `npm ci`（`apps/web`）：退出码 0，锁文件可复现安装。
2. `npm run lint`（`apps/web`）：退出码 0；有一条 React `set-state-in-effect` warning，不影响退出码，待后续 UI 代码质量复核。
3. `npm run test:run`（`apps/web`）：退出码 0，1 个测试文件、4 个测试通过。
4. `npm run build`（`apps/web`）：退出码 0，Vite production build 成功。
5. `./mvnw -B -ntp test`（`apps/server`）：退出码 0，5 个单元测试通过。
6. 设置 `TEST365ALM_IT_DATASOURCE_URL/USERNAME/PASSWORD` 后执行 `./mvnw -B -ntp -Pintegration verify '-Dbuild.commit=local-r02-integration'`：退出码 0，Surefire 5 通过，Failsafe 2 通过；首次应用迁移后再次运行显示无迁移需要执行。
7. `docker compose --env-file .env -p test365alm-r02 up -d postgres`：退出码 0，容器健康，端口为 `127.0.0.1:54329`。
8. Python 规划工具（R01 已复跑）：`python -m unittest discover -s tools/tests -v` 15 通过；`python tools/validate_package.py` 通过；`python tools/p0_health_check.py` 退出码 0 但继续报告 P0 输入缺失/产品验收未执行，不能解释为业务验收通过。

曾出现并已修正的执行问题：从仓库根目录调用 Maven 因无根 POM 退出 1；PowerShell 未引用 `-Dbuild.commit` 时参数解析失败；Boot 4 测试未提供 `TestRestTemplate`，集成测试改用 JDK `HttpClient`。这些不是最终代码验证结果，保留在审计轨迹中。

## 提交、远端和 CI

- 代码提交 SHA：`0272b06`（包含最后一次代码/配置变更；进度文档不在此 SHA 中）。
- 最终交付 SHA（含本记录）：本轮最终回复给出；不把自身 SHA 写回文档，避免自引用。
- 远端推送：已成功推送；最终远端分支 SHA 以本轮最终回复中的 `git ls-remote` 核对结果为准。
- PR：已创建 [#1](https://github.com/fangzhiy/Test365Alm/pull/1)，base 为实际存在的 `chore/r01-status-001`，未自动合并。
- CI：GitHub Actions run [35709638396](https://github.com/fangzhiy/Test365Alm/actions/runs/35709638396) 已对代码/文档提交 `3bcb16b5bc7a107ea2adfa526cf9f70c5361d756` 完成；三个 job 均 success。最终文档提交会触发新的 run，需按最终 SHA 再核对。

## 未完成、风险和需用户决定

- A04/A05 和 A06 的本轮本机证据已完成；浏览器使用隔离本地页，未访问用户已有 Edge 页面。
- Java21、Node24 未安装；需要用户提供或允许安装目标运行时，才能做本机目标版本复跑。
- 无旧 ALM 版本、Edition、客户端/扩展及三类脱敏样本；B01-B04/B02 继续为待补充/待验证，不阻塞本轮工程底座。
- 不删除现有 Compose 数据卷；若要清理仅针对 `test365alm-r02` 隔离资源并先确认。

## 下一批建议

身份与项目权限最小切片；随后实现登录后创建需求的真实业务流程。两项都应先更新契约与验收，再分薄片实施，不把本轮底座视为完整业务能力。
