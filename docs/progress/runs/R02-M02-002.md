# R02-M02-002 修复 R02 审核问题与回归证据

## 任务与边界

- 任务编号：`R02-M02-002`
- 目标：修复前端刷新竞态和无效响应误报，区分 readiness 的数据库连接/必要结构状态，补齐真实 PostgreSQL 故障恢复验证、本机回环边界、CI 证据和可操作文档。
- 不在范围：登录、身份与权限、需求/用例/缺陷、执行 Agent、OTA/COM、电子签名、AI、生产部署和 M03。
- 记录性质：本记录是执行证据，不是外部审核通过声明。

## 起始状态

- 仓库：`https://github.com/fangzhiy/Test365Alm.git`
- 开始前执行 `git fetch origin --prune`；远端 PR #1 仍为 open，head `feat/r02-m02-001`，base `chore/r01-status-001`，未合并。
- 起始分支：`feat/r02-m02-001`
- 起始提交：`e10380ba1be542389575a915bc6e3fae8699d81b`
- 起始工作区：fetch 后干净；未发现审核基线之后的远端提交或用户未提交修改。
- 代码提交：`91d4aff9fce5d792611803565645f893b3dded04`（本轮最终代码验证对应 SHA；进度文档不包含在该代码提交中）。

## 工具链、配置与修改

| 项目 | 实际值 | 结论 |
|---|---|---|
| 本机 Java | `17.0.2` | 实际可用；CI 使用 Java 21，本机未把目标版本写成已验证 |
| 本机 Node/npm | `v26.0.0` / `11.12.1` | 实际可用；CI 使用 Node 24.x |
| Docker/Compose | Engine `29.7.2` / Compose `v5.4.0` | 可运行隔离 PostgreSQL |
| Maven | 系统 `3.9.14`；Wrapper `3.9.16` | 命令使用 Maven Wrapper |
| 编译目标 | Maven `release 17` | 保留既有配置；CI 在 Java 21 上验证该目标 |
| PostgreSQL | `17.11`，digest `sha256:f4c66b820c6f974249089d3d16d86a3698eae11e8746eb6644b2271031e91232` | Compose 项目 `test365alm-r02`，宿主机仅 `127.0.0.1` |

主要修改：

- `apps/web/src/App.tsx`：请求轮次、AbortController、超时/卸载清理；版本和健康 JSON/字段/枚举运行时校验；合法 readiness 503 保留数据库/迁移详情，旧请求不能覆盖新状态。
- `apps/web/src/App.test.tsx`：两种响应乱序、无效 JSON/空对象/null/缺字段/错误类型、超时、卸载、StrictMode、失败后恢复，共 14 个测试。
- `DatabaseReadinessProbe`/`ReadinessResult`/Controller 测试：连接失败 `DOWN/UNKNOWN`；数据库可连接但必要结构缺失 `UP/NOT_APPLIED`；结构无法检查 `UP/UNKNOWN`；完整结构和 Flyway V1 成功记录才是 `APPLIED`。
- `PlatformDatabaseIT`：真实 PostgreSQL 中临时删除隔离库 `platform_metadata`，断言 ready 503 且 `database=UP, migration=NOT_APPLIED`，finally 恢复表和元数据；既有迁移重跑/保留数据测试保留。
- `application.properties`、`vite.config.ts`、`compose.yaml`、`.env.example`：默认回环绑定、统一 Compose 名称、同一 `.env` 派生端口/凭据；`tools/import_dev_env.ps1` 不覆盖已有 `.env`。
- `tools/verify_r02_readiness.py`：启动隔离数据库和打包后端，实际检查回环监听、停库期间 live/ready、恢复期间 ready；只使用有界 HTTP 超时，不删除本地卷。
- `.github/workflows/ci.yml`：Java 21/Node 24/Python 3.12 版本记录；前端、后端、规划工具报告上传；独立 readiness-recovery job 使用临时 Compose 资源并在 finally 清理卷。
- `README.md`、`AGENTS.md`、`contracts/platform-health.json`、`docs/adr/007-platform-health-contract.md`：更新启动边界、契约语义和只读探测约束；移除仓库未创建的过期描述。

## 红测与修复证据

- 前端旧实现红测：`npm run test:run -- src/App.test.tsx`，13 个场景中 5 通过、4 失败（乱序覆盖、无效响应、卸载清理、StrictMode），退出码 1；随后补充 1 个超时场景并在修复后达到 14/14。
- 后端旧实现红测：`./mvnw.cmd -B -ntp '-Dtest=PlatformHealthControllerTest,DatabaseReadinessProbeTest' test` 在新增测试编译阶段因缺少新结果工厂方法退出码 1；修复后 targeted tests 7/7 通过。

## 实际验证命令与结果

以下最终代码检查均对应 `91d4aff9fce5d792611803565645f893b3dded04`；报告文件由构建工具生成在本地工作区，未把大日志提交仓库。

| 命令 | 退出码/数量 | 结果与证据 |
|---|---:|---|
| `cd apps/web; npm ci` | 0 | 锁文件安装成功 |
| `cd apps/web; npm run lint` | 0 | 通过；`App.tsx:105` 保留一条 `react(set-state-in-effect)` warning，未用跳过规则掩盖 |
| `cd apps/web; npm run test:run -- --reporter=verbose` | 0；14/14 | 通过，1 文件、14 测试；本地终端输出，CI `frontend-evidence` artifact 也保存 |
| `cd apps/web; npm run build` | 0 | `tsc -b` 与 Vite 生产构建通过 |
| `cd apps/server; .\\mvnw.cmd -B -ntp -Pintegration verify '-Dbuild.commit=91d4aff'` | 0；Surefire 9、Failsafe 3 | 通过；报告 `apps/server/target/surefire-reports/`、`apps/server/target/failsafe-reports/`；日志显示 Flyway 已是 up-to-date |
| `python -m py_compile tools/verify_r02_readiness.py` | 0 | 通过 |
| `docker compose --env-file .env -p test365alm-r02 config --quiet` | 0 | Compose 配置和项目名通过 |
| `python -u tools/verify_r02_readiness.py --wait-seconds 45` | 0 | `listener ['127.0.0.1']`；startup live 200/ready 200；停库 live 200/ready 503；恢复 ready 200，后端进程未重启 |
| `git diff --check` | 0 | 无空白错误 |

一次并发误操作也如实保留：故障脚本与 Maven 同时操作同一个本地 JAR 时，Maven 重打包退出 1、脚本未得到 ready 503；停止并发后按顺序重跑，上表结果通过。该次不作为最终业务失败结论。

规划工具在进度文档创建前曾因 README 新链接目标不存在而出现 1 个失败；文档创建后必须重新执行，不能用串联命令的最后一个退出码掩盖它。

## B01-B09 验收矩阵

| 编号 | 结果 | 证据/限制 |
|---|---|---|
| B01 | PASS | 前端两种乱序测试均证明旧慢响应不能覆盖最新失败或成功。 |
| B02 | PASS | HTML/解析失败、空对象、null、缺字段、错误类型和健康字段错误均不显示正常；合法响应可恢复。 |
| B03 | PASS | 14 个前端测试覆盖超时、卸载 abort/定时器清理、StrictMode、连续刷新和失败后恢复。 |
| B04 | PASS（运行期）；NOT_RUN（故意失败迁移的独立启动注入） | 真实 PostgreSQL 缺结构返回 503 + `UP/NOT_APPLIED`；连接断开返回 `DOWN/UNKNOWN`；Flyway 缺失位置配置为 fail-fast，但本轮未破坏性注入启动失败。 |
| B05 | PASS | `verify_r02_readiness.py` 对隔离 Compose 数据库实际停/起，后端进程保持不重启。 |
| B06 | PASS | 后端脚本实际观测 `127.0.0.1` listener；Spring/Vite 默认 host 和 Compose 宿主端口均为回环。 |
| B07 | PASS | README 启动/停止/排障可操作；同一 `.env` 派生配置；PowerShell import 实测不覆盖已有配置；仓库未跟踪 `.env`。 |
| B08 | PASS | 前端 14、后端单元 9、真实 PG 集成 3、规划工具 15（文档创建后复跑）均有非零测试计数。 |
| B09 | NOT_RUN（待远端） | CI workflow 已更新并包含报告 artifact；须推送本轮提交后核对 GitHub Actions 的实际 SHA、URL 和 job 结果。 |

## 未完成、未验证和风险

- B04 的独立“故意失败迁移导致启动失败”注入未运行；原因是不能破坏共享开发库，现有 `spring.flyway.fail-on-missing-locations=true` 和 Flyway 启动日志仅作为配置/正常迁移证据。
- 本机 Java 17、Node 26 与目标 Java 21、Node 24 有差异；目标运行时由既有 CI 选择，需在 CI 结果中核对。
- GitHub Actions 远端执行、artifact 和本轮最终 SHA 尚未在本记录创建时取得；不把新增 YAML 写成远端已通过。
- 未提供旧 ALM 版本/Edition、扩展及授权脱敏样本，继续保留 P0 B01-B04/B02 输入阻塞；本轮不宣称兼容认证或 M02/P0 完成。

## 后续

等待远端 CI 与外部审核；下一批仍建议先做身份与项目权限最小切片，再做登录后创建需求的真实业务流程。本轮不自动进入 M03。
