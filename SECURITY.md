# Test365Alm 安全约束

当前仓库为规划文档和辅助工具，尚未提供业务系统。不要在 Issues、Git 或公开附件中提交账号密码、访问令牌、生产数据库、客户附件或可识别个人的信息。真实测试证据应使用授权受限存储，仓库保存引用和哈希。

发现凭据泄漏先撤销/轮换；仅删除当前文件不能消除 Git 历史。安全缺陷由项目指定负责人通过公司批准的私有渠道处理；当前尚未任命具体邮箱或值班人，不虚构联系地址。

发布脚本不接收或打印令牌，只调用用户已安装并授权的 GitHub CLI。默认只预览，明确 --execute 后才创建私有仓库并推送。
