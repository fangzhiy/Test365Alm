# R03-M03-001-FIX02 — HTTP OIDC 回调拒绝与主体不变证据

## 1. 任务与起点

- 任务：`R03-M03-001-FIX02`，只补齐 E05/F03 的直接证据；不重做 FIX01，不开发项目权限、RLS、MFA 或后续模块。
- 仓库：<https://github.com/fangzhiy/Test365Alm.git>。
- 分支：`feat/r03-m03-001`；PR [#2](https://github.com/fangzhiy/Test365Alm/pull/2) 仍为 Open，base 为 `feat/r02-m02-001`；PR #1 仍未合并。
- 审核基准/起始提交：`d8240fd0f8f006326f7a0188d900dca4f119917e`。开始前重新 fetch，未发现后续远端提交，工作区原本干净。
- 代码与测试提交：`d52598d8d1224e1fad82e853be96024d243d04bd`。
- 交付文档提交：本记录不自引用自身 SHA；最终交付 SHA 在任务回复中给出。

## 2. 实际完成内容

| 文件 | 修改原因 |
| --- | --- |
| `apps/server/src/test/java/com/test365alm/server/identity/OidcCallbackSecurityIT.java` | 新增真实应用 HTTP 授权码、state、nonce、PKCE、token、JWKS、OIDC 用户加载和 `/api/v1/me` 集成测试；Testcontainers 使用固定摘要 PostgreSQL 临时库和运行/迁移角色分离。 |
| `tools/verify_r03_oidc_http_report.py` | 严格验证 Failsafe 报告确实包含 7 个指定 HTTP 回调用例，且失败、错误、跳过均为 0。 |
| `tools/tests/test_r03_ci_evidence.py` | 增加报告完整性离线回归，覆盖完整报告和缺失用例拒绝。 |
| `.github/workflows/ci.yml` | server job 安装 Python 3.12，集成测试后执行 HTTP 回调报告门槛并上传报告；保留原六个 job 与其失败语义。 |
| `README.md`、`AGENTS.md` | 增加真实 HTTP OIDC 集成命令、报告门槛和测试隔离约束。 |
| `docs/adr/011-r03-http-oidc-callback-evidence.md` | 记录本轮协议测试夹具、nonce 语义、数据库快照和安全边界。 |

测试 IdP 只监听回环随机端口；每类场景使用独立 Cookie 容器、一次性授权码和合成 subject。token 端点实际检查 client、grant、redirect URI、一次性 code 和 PKCE S256；测试从应用真实授权重定向读取 state、nonce、redirect URI、challenge。五个非法 token 只分别改变错误签名、issuer、audience、过期或 nonce。失败后仍用原 Cookie 请求 `/api/v1/me`，并以独立 owner 连接逐字段比较 `principal` 快照；未改动生产认证过滤器链或主体仓储。

## 3. 工具与配置事实

- 本机 Windows：Java `17.0.2`，编译目标 `--release 17`；Node `v26.0.0`，npm `11.12.1`；Python `3.12.8`；系统 Maven `3.9.14`；Docker Engine `29.7.2`，Docker Desktop Linux context，Compose `v5.4.0`。
- CI 目标运行时仍为 Temurin Java 21、Node 24、Python 3.12；本机 Maven Wrapper 在 Windows 继续报既有 `Cannot index into a null array`，没有改 wrapper 或把本机 Java 17 写成 Java 21 已验证。
- 测试 PostgreSQL：`postgres:17.11@sha256:f4c66b820c6f974249089d3d16d86a3698eae11e8746eb6644b2271031e91232`；Testcontainers 为 `2.0.5`。测试动态注入临时 JDBC/Flyway 地址，不读取日常 `.env`，由 Testcontainers/Ryuk 清理。
- 本机普通 `npm ci` 首次因系统全局 npm cache `C:\App\nodejs\ins\node_cache` 权限返回 `EPERM`；改用工作区被忽略的 `apps/web/.npm-cache/r03-fix02` 隔离 cache 后 `npm ci` 成功。未修改系统 npm 配置。

## 4. 实际测试与证据

| 检查 | 命令/退出码 | 实际结果与证据 |
| --- | --- | --- |
| Python 工具与规划回归 | `python -m unittest discover -s tools/tests -v` / exit `0` | `39/39` 通过；负向测试在 stderr 打印预期 `FAIL` 文本，但 unittest 总结果为 OK。 |
| HTTP 报告门槛 | `python tools/verify_r03_oidc_http_report.py apps/server/target/failsafe-reports` / exit `0` | `OidcCallbackSecurityIT tests=7 failures=0 errors=0 skipped=0 cases=7`；本地报告为 `apps/server/target/failsafe-reports/TEST-com.test365alm.server.identity.OidcCallbackSecurityIT.xml`（构建目录未提交）。 |
| 后端最终构建/单元/真实 PostgreSQL 集成 | `mvn -B -ntp '-Dmaven.repo.local=.maven-repo' -Pintegration verify '-Dbuild.commit=d52598d8d1224e1fad82e853be96024d243d04bd' '-DargLine=-Djava.io.tmpdir=.../target/tmp-jna'` / exit `0` | Surefire `17/17`；Failsafe `15/15`（OIDC HTTP `7`、平台隔离 `1`、平台数据库 `6`、principal 迁移 `1`），失败/错误/跳过均 `0`；报告在 `apps/server/target/{surefire,failsafe}-reports/`。 |
| 前端依赖 | `npm ci --cache .npm-cache/r03-fix02` / exit `0` | lockfile 可复现安装，126 packages。 |
| 前端 lint | `npm run lint` / exit `0` | 通过；保留既有 `react(set-state-in-effect)` warning，非本轮阻断。 |
| 前端测试 | `npm run test:run -- --reporter=verbose` / exit `0` | 2 files、`21/21` 通过，包含登录状态面板现有回归。 |
| 前端构建 | `npm run build` / exit `0` | TypeScript/Vite 构建成功。 |
| 差异/秘密边界 | `git diff --check` / exit `0`；`git ls-files .env.r03 local-evidence/r03/realm.json` 无输出 | 未跟踪本地 `.env.r03`、realm 或客户数据；本轮日志/制品不包含 Cookie、token、私钥。 |

首次在 nonce 尚未修正的实现上运行时，HTTP 集成 7 项中合法对照和恢复场景失败（Spring Security `invalid_nonce`）；随后依据真实授权请求参数修正测试夹具为回显实际发送的哈希 nonce，再次运行完整 7/7 通过。该失败保留为本轮调试事实，没有把它写成安全通过。

## 5. G01—G06 与 E05/F03

| 验收项 | 结果 | 证据/边界 |
| --- | --- | --- |
| G01 合法 HTTP 回调、`/me=200`、principal 持久化 | **PASS** | 同一真实应用回调链路和临时 PostgreSQL；合法场景 `legalTokenCompletesHttpCallbackAndPersistsPrincipal`。 |
| G02 五类非法 token 经真实 token 响应和回调被拒，原因可区分 | **PASS** | 五个独立用例名和单一 claim 变体：signature、issuer、audience、expired、nonce；token 兑换计数为 1 且协议校验通过，拒绝发生在应用 OIDC 校验链。 |
| G03 同 Cookie `/api/v1/me` JSON 401 | **PASS** | 五个非法场景均断言 302 失败处理、同 Cookie `/api/v1/me` 为 JSON `401/UNAUTHENTICATED`。 |
| G04 完整 principal 快照不变 | **PASS** | 五个场景均比较 ID、issuer、subject、display_name、disabled_at、created_at、updated_at，且断言非法 subject 不存在。 |
| G05 失败后重新合法授权恢复 | **PASS** | `failedCallbackDoesNotPoisonClientAndFreshLegalAuthorizationRecovers`：失败后新授权、新 code、新 Cookie 流程 `/me=200`；数据库与 IdP 均为 Testcontainers/回环合成资源。 |
| G06 原六 job、新 HTTP 集成和代码提交 CI | **PASS（代码提交）** | `d52598d` Push run [35963117055](https://github.com/fangzhiy/Test365Alm/actions/runs/35963117055) 与 PR run [35963119731](https://github.com/fangzhiy/Test365Alm/actions/runs/35963119731) 均完成且六 job success，backend-evidence 含 Failsafe 报告；PR merge ref 当时为 `5ee8fd859802cb5f974d14356b7f98aae6814c77`。文档交付提交会触发新的正常 Push/PR 检查，其最终状态在回复中单独报告，不为追写最新 CI 修改本记录。 |
| E05 本轮定义范围 | **PASS** | G01—G06 的 HTTP/数据库直接证据齐全；不等价于全部 OIDC 标准、生产 IdP 或完整 M03 安全审查。 |
| F03 本轮定义范围 | **PASS** | 与 E05 同一组真实回调、会话和主体快照证据；FIX01 的 provider 层测试和浏览器测试历史继续保留。 |

## 6. 未完成、风险与需用户处理

- PR #2 仍未合并且依赖 PR #1；本轮不自动合并、部署或修改分支保护。
- R03 仍不是完整 M03：项目/成员权限、RLS、跨租户、MFA、多节点会话、实时 IdP 撤权、完整审计和生产 HTTPS/秘密管理不在本轮。
- Windows Maven Wrapper、Java 17/Node 26 与目标 Java 21/Node 24 的本机差异继续保留；CI 已负责目标运行时验证。
- 本轮没有访问旧 ALM、客户数据或生产数据库；旧版本/Edition/兼容样本和完整 P0 输入仍待补充。
- CI 结果只代表对应 checkout 和本轮隔离测试资源；不把 PR merge CI 写成分支 head SHA，也不把文档提交的 CI 状态提前写成已完成。
