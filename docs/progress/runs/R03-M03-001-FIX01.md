# R03-M03-001-FIX01 — OIDC CI 与验收补修

## 范围与提交身份

- 仓库：`https://github.com/fangzhiy/Test365Alm.git`；分支 `feat/r03-m03-001`，PR [#2](https://github.com/fangzhiy/Test365Alm/pull/2) 依赖尚未合并的 PR #1，base 为 `feat/r02-m02-001`。没有合并、强推、改默认分支、部署或恢复定时任务。
- FIX01 审核起点为 `f97967a19a349d8139941d9f92288abea5a2e1a6`，原代码 SHA `a1f6e89c9a8f384538087af661d5d690e7130daf`。开始前重新核对远端和工作区；保留该历史记录和 R02 的代码/文档。FIX01 测试对应代码 SHA：`2992db71afa5c63a3bba24aaf9c5305c9df52ef6`。本记录的交付 SHA 在最终回复给出，不自引用。
- 本轮只修复 CI Keycloak 导入、认证负向校验和会话/IdP 故障验收；不开发项目权限、RLS、MFA，也不复做 R02。

## 实际修复与证据

| 文件 | 原因与内容 |
|---|---|
| `.github/workflows/ci.yml` | Linux CI 在 Keycloak up 前将本轮 realm 导入文件限定给镜像 UID 1000 读取（0400），检查 issuer、文件可读和镜像身份；失败时清理前保存容器状态/日志；上传仅限脱敏白名单文件，报告必须有 5 个实际浏览器测试、零失败/错误/跳过。CI 专属 1 分钟会话超时。 |
| `tools/prepare_r03_dev.py`、`compose.r03.yaml` | 明示 realm 与 `.env.r03` 均含随机测试秘密；添加不可复用 `R03_RUN_ID` 资源标签。 |
| `tools/redact_r03_logs.py`、`tools/verify_r03_report.py`、`tools/tests/test_r03_ci_evidence.py` | 脱敏容器日志；严格验证报告计数、失败/错误/跳过与缺报告，离线回归。 |
| `apps/server/src/main/resources/application.properties`、`apps/server/src/test/java/com/test365alm/server/identity/OidcValidationTest.java` | 显式可配置空闲超时；经过 Spring Security 实际 OIDC authentication provider 和本地 JWKS 的有效对照、错误签名/issuer/audience/过期/nonce 五项拒绝，断言无用户加载（主体写入在其下游）。 |
| `apps/web/e2e/ownedR03.ts`、`apps/web/e2e/oidc.spec.ts` | 只有本轮 CI 标签、项目及 Docker 引擎匹配才停启临时 Keycloak 或停用测试主体。真实浏览器验证空闲过期、旧 Cookie 重放、停用后重新登录拒绝、IdP 中断/恢复；关闭页面避免健康轮询续期，兼容 IdP 已有 SSO 会话。 |
| `README.md`、`AGENTS.md`、`docs/development-status.md`、本记录 | 记录 Linux 导入文件权限、测试隔离、IdP 故障策略及历史失败与当前边界。 |

原失败并非浏览器登录断言：`a1f6e89` 的 [Push 35849698770](https://github.com/fangzhiy/Test365Alm/actions/runs/35849698770) 与 [PR 35849942642](https://github.com/fangzhiy/Test365Alm/actions/runs/35849942642) 在 discovery 前退出，浏览器报告不存在。原因是 POSIX 生成的 0600 realm 文件属 runner，而锁定 Keycloak 镜像以 UID 1000 运行。最小权限修复 `276185e` 的 [Push 35942860030](https://github.com/fangzhiy/Test365Alm/actions/runs/35942860030) 与 [PR 35942863496](https://github.com/fangzhiy/Test365Alm/actions/runs/35942863496) 六个 Job 均成功，真实登录执行，支持该根因判断。PR 原失败实际 checkout 为 `464f1805007f38dde7595997393b7258db4d9aca`，并非分支 head。

随后 `4a63d91` 的 [PR 35944232951](https://github.com/fangzhiy/Test365Alm/actions/runs/35944232951) 浏览器 5 项中 2 通过、2 失败、1 错误；`897acce` 的 [PR 35944821893](https://github.com/fangzhiy/Test365Alm/actions/runs/35944821893) 浏览器 5 项中 3 通过、1 失败、1 错误，实际 merge checkout 为 `be8e10aac709253cc0168a0545408d26bf048de1`。这两次未写成通过。修复异步退出、会话有效空闲期和 Keycloak 恢复后 SSO 自动回跳后，`0c465a0` 的 [Push 35945370449](https://github.com/fangzhiy/Test365Alm/actions/runs/35945370449) 和 [PR 35945374539](https://github.com/fangzhiy/Test365Alm/actions/runs/35945374539) 六个 Job 全通过；PR 浏览器报告 `tests=5 failures=0 errors=0 skipped=0`，实际 merge checkout 为 `b9c15fdc1bc9da657ddd28e29b45264d45dff127`。最终测试增强提交 `2992db7` 的 [Push 35945841031](https://github.com/fangzhiy/Test365Alm/actions/runs/35945841031) 与 [PR 35945844209](https://github.com/fangzhiy/Test365Alm/actions/runs/35945844209) 也均六个 Job 成功；分支 head `2992db71afa5c63a3bba24aaf9c5305c9df52ef6`，PR merge checkout `e98b22d45ae897f0b7ca7d085990d5645635c689`，应用构建提交为 PR checkout SHA。PR run 有六件报告制品，认证制品 [r03-oidc-browser-evidence](https://github.com/fangzhiy/Test365Alm/actions/runs/35945844209/artifacts/10786398914)；不含 `.env.r03` 或 realm 文件。

## 实际命令与环境

本机 Windows：Java 17.0.2、Node 26.0.0、Python 3.12.8、系统 Maven 3.9.14；Maven 编译 `--release 17`。CI Ubuntu 使用 Temurin Java 21、Node 24、Python 3.12 和 Linux Maven Wrapper。本机 `mvnw.cmd` 历史报 `Cannot index into a null array`，本轮按既有已验证的系统 Maven 命令，不声称 Windows Wrapper 已修复。

| 命令/检查 | 结果 | 证据 |
|---|---|---|
| `python -m unittest discover -s tools/tests -q` | exit 0，38/38 | 本机控制台；其中预期拒绝分支输出 `FAIL` 字样不代表 unittest 失败 |
| `python tools/validate_package.py` | exit 0，规划包 `ok=true`、产品验收 `NOT_EXECUTED` | 本机控制台，不等于业务测试 |
| `cd apps/web; npm run lint; npx playwright test --list; npm run test:run` | 各 exit 0；E2E 发现 5 个，前端 21/21；lint 一条既有 `react(set-state-in-effect)` warning | 本机控制台、CI `frontend-evidence` |
| `cd apps/server; mvn -B -ntp '-Dmaven.repo.local=.maven-repo' test` | exit 0，17/17、skipped 0 | `apps/server/target/surefire-reports/`（本地忽略）、CI `backend-evidence` |
| `cd apps/server; mvn -B -ntp '-Dmaven.repo.local=.maven-repo' -Pintegration verify '-Dbuild.commit=local-r03-fix01'` | exit 0，单元 17/17、真实 PostgreSQL 集成 8/8、skipped 0 | `apps/server/target/{surefire,failsafe}-reports/`（本地忽略）、CI `backend-evidence` |
| `git ls-files .env.r03 local-evidence/r03/realm.json`、`git diff --check` | exit 0，前者无输出，未跟踪这两个秘密文件 | 本机控制台；CI 仅上传白名单脱敏制品 |
| CI Playwright 真 Keycloak 5 场景 | `2992db7` PR run exit 0，5/5、failures/errors/skipped 均 0 | [PR run 35945844209](https://github.com/fangzhiy/Test365Alm/actions/runs/35945844209) 的 `r03-oidc-browser-evidence`、checkout 报告 |

当前本机 `.env.r03` 是既有被忽略配置，没有 CI 本轮资源标签；未对它执行停库、停 IdP 或停用主体。真实故障验收只在 GitHub 临时 CI 项目完成。CI 会话策略：新登录依赖 IdP、故障时失败；既有本地会话在本地 TTL 内仍可访问并可 CSRF 退出；IdP 恢复后可以重新登录。不等于实时撤权或全局单点退出。

## F01–F07 与 E01–E12

| 项 | 状态 | 证据或边界 |
|---|---|---|
| F01 | PASS | `276185e` 后 Linux Keycloak 正常导入、discovery/issuer 与文件可读检查通过；原缺报告失败保留。 |
| F02 | PASS | `0c465a0` PR 真浏览器登录→持久主体→`/me`→CSRF 退出通过。 |
| F03 | NOT_RUN | 五个恶意令牌经真实 provider/JWKS 拒绝且用户加载为零；尚未逐个经 HTTP 回调独立断言 `/me` 与数据库行数，不能将部分证据写成完整逐项验收。 |
| F04 | PASS | CI 真实 1 分钟空闲到期后旧会话 401，临时库停用使既有会话失效且再次登录失败；不用日常库。 |
| F05 | PASS | CI 缺失/错误 CSRF 均 403 且原会话保持；合法退出后旧 Cookie 独立重放 401。 |
| F06 | PASS | CI 临时停启 Keycloak，故障中新登录不可完成、既有会话可用且可退出，恢复后重新登录成功。 |
| F07 | PASS | 最终代码 `2992db7` 的 Push/PR 六项 CI 与六件白名单制品均成功；文档交付提交将另行触发 CI，结果在回复单独核对。 |
| E01 | PASS | CI 干净 Linux checkout、固定镜像、隔离回环端口和真实四服务启动通过；本机目标 Java21/Node24 不冒充已安装。 |
| E02 | PASS | 真实 Keycloak 授权码浏览器登录，`/me` 返回稳定 principal UUID。 |
| E03 | PASS | 匿名、伪造身份头、失效会话由后端/浏览器测试返回 JSON 拒绝。 |
| E04 | PASS | Testcontainers PostgreSQL 重复/并发、同名不同合法身份不合并（原 R03 证据保留）。 |
| E05 | NOT_RUN | 对应 F03；provider 五项加 state/回调/PKCE 已验证，逐项 HTTP 会话及数据库无写仍缺直接断言。 |
| E06 | PASS | 真实浏览器 HttpOnly/SameSite=Lax、会话轮换，API 与前端存储无认证 token。 |
| E07 | PASS | 对应 F05；退出后页面可显式再登录。 |
| E08 | PASS | 对应 F04。 |
| E09 | PASS | 对应 F06；不包括实时 IdP 撤权。 |
| E10 | PASS | 原 R03 空库、V1→V2、重启稳定 UUID、运行/迁移角色分离及故意失败 V3 迁移证据保留。 |
| E11 | PASS | 原五项加认证 Job 在 `2992db7` Push/PR 均成功，单元 17、真实 PostgreSQL 集成 8、前端 21、Python 38、浏览器 5，真实停库恢复与失败迁移 Job 均成功。 |
| E12 | PASS | 分支 SHA、PR merge checkout、各 Job 结果、六件脱敏制品及测试数量可查；交付文档提交 SHA 与对应 CI 将在最终回复核对，不在本记录中自引用。 |

## 剩余风险与用户处理

- E05/F03 的每个非法令牌没有完整 HTTP 回调后的 `/me` 与真实数据库不变的独立断言；provider 拒绝和主体加载零调用只是部分证据。此项需后续隔离协议级故障注入，不得对外宣称完整 OIDC 安全认证。
- 本机 Windows Wrapper 调用偏差、Java17/Node26 与 CI Java21/Node24 不同；既有 lint warning 与 GitHub actions deprecation warning 登记后续，不为本轮无关升级整套依赖。
- PR #1 尚未合并；PR #2 依赖其基线。需外部审核及用户决定是否合并；生产 IdP、HTTPS、权限/RLS、集群会话等后续处理，不在本轮授权内。
