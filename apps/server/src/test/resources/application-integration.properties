# The integration test classes inject a Testcontainers-owned JDBC URL through
# @DynamicPropertySource.  Deliberately do not read the developer .env here.
spring.datasource.hikari.connection-timeout=2000
spring.datasource.hikari.validation-timeout=2000
spring.flyway.clean-disabled=true
